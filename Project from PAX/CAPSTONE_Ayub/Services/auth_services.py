class AuthService:

    def register_user(self, username, password):
        print(f"User '{username}' registered successfully (dummy).")

    def login_user(self, username, password):
        print(f"User '{username}' logged in successfully (dummy).")
        return True
